{"S00":"WHAT TO DO (MANUAL — you do this, no Claude Code needed)\n\n1. Back up externally before touching anything:\n   - memories/memories pedro - indesign copy.pdf  (16MB)\n   - memories/*.idml  (765KB)\n   - memories/1934 to now.zip\n   - memories/*.epub\n   - blueprint/pedro_blueprint.docx\n   - journal/pedro_journal.docx\n   - any health/exportSportData_*.zip\n\n2. Decide what stays vs what goes out of repo:\n   - Recommendation: keep .idml (irreplaceable source).\n   - Recommendation: move 16MB PDF out of repo entirely.\n\n3. Rename files with spaces to kebab-case.\n\n4. Optional: enable Git LFS for source-files/*.pdf and *.idml.\n\n5. Commit on branch prep/s00-manual-binary-cleanup.\n\n6. Open PR, self-review, squash merge, delete branch.","S01":"STANDING INSTRUCTIONS\n\nBefore starting: git checkout main && git pull origin main in pcblueprint-archive.\nThen: git checkout -b claude/s01-ia-spec.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Append an entry to pcblueprint-archive/sessions/session-log.en.md AND session-log.pt.md: date, session ID, repos touched, what changed, follow-ups.\n- Push branch: git push -u origin claude/s01-ia-spec.\n- Open PR titled \"S01 — IA Spec\" with short body (scope, verification, time).\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S01 — IA Spec — pcblueprint-archive — ~45 min\n\nDesign-only. NO FILE MOVES.\n\nGoal: produce meta/ia-spec.md defining the new information architecture.\n\nDeliverables:\n\n1. NEW FILE meta/ia-spec.md containing:\n   - Lifecycle buckets: raw/, records/, writing/, source-files/, _legacy/, meta/, scripts/, sessions/, claude-code-prompts/.\n   - Full target tree, 2 levels deep, with a migration table mapping every existing path → new path.\n   - _legacy/ retention policy (date-stamped subfolders, 12-month review).\n   - Data dictionary: one-line purpose per folder.\n\n2. Update README.md to reference meta/ia-spec.md as source of truth.\n\n3. List path-mapping ambiguities at the bottom for human decision.\n\nVerification: spec file exists and every current top-level path appears in the migration table. No other files modified.\n\n---END S01---","S02a":"STANDING INSTRUCTIONS\n\nBefore starting: git checkout main && git pull origin main in pcblueprint-archive.\nThen: git checkout -b claude/s02a-memories-wip-legacy.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Append an entry to pcblueprint-archive/sessions/session-log.en.md AND session-log.pt.md: date, session ID, repos touched, what changed, follow-ups.\n- Push branch: git push -u origin claude/s02a-memories-wip-legacy.\n- Open PR titled \"S02a — Consolidate memories-wip + _legacy READMEs\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S02a — Consolidate memories-wip + _legacy READMEs — pcblueprint-archive — ~40 min\n\nChanges:\n\n1. Merge memories-wip/ into memories/drafts/ using git mv. Do not lose content.\n2. Add README.md to EVERY existing _legacy/ folder. Each README must state:\n   - Purpose of the folder\n   - Retention policy (link back to meta/ia-spec.md)\n   - Date moved\n\nVerification: git status shows only renames and new README.md files. No content loss (compare sizes pre/post).\n\n---END S02a---","S02b":"STANDING INSTRUCTIONS\n\nBefore starting: git checkout main && git pull origin main in pcblueprint-archive.\nThen: git checkout -b claude/s02b-source-files.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Append an entry to pcblueprint-archive/sessions/session-log.en.md AND session-log.pt.md: date, session ID, repos touched, what changed, follow-ups.\n- Push branch: git push -u origin claude/s02b-source-files.\n- Open PR titled \"S02b — Create source-files/ + Move Binaries\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S02b — Create source-files/ + Move Binaries — pcblueprint-archive — ~30 min\n\nChanges:\n\n1. Create source-files/ at root with a README.md describing its purpose (per ia-spec.md).\n2. git mv the following into source-files/:\n   - blueprint/pedro_blueprint.docx\n   - journal/pedro_journal.docx\n   - any remaining memories/*.idml\n   - memories/1934-to-now-archive.zip (renamed if needed per S00)\n3. DELETE generated duplicates where markdown is canonical:\n   - memories/*.epub\n   - memories/*.pdf (where a markdown source exists)\n   - KEEP .idml (irreplaceable) in source-files/.\n4. Update scripts/migrate-to-legacy.ts if it references any moved paths.\n\nVerification: git status shows only the expected moves and deletes. No markdown content lost.\n\n---END S02b---","S03a":"STANDING INSTRUCTIONS\n\nBefore starting: git checkout main && git pull origin main in pcblueprint-archive.\nThen: git checkout -b claude/s03a-health-reorg.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Append an entry to pcblueprint-archive/sessions/session-log.en.md AND session-log.pt.md: date, session ID, repos touched, what changed, follow-ups.\n- Push branch: git push -u origin claude/s03a-health-reorg.\n- Open PR titled \"S03a — Health Folder Reorg + Resolve /sports\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S03a — Health Folder Reorg + Resolve /sports — pcblueprint-archive — ~40 min\n\nChanges:\n\n1. Resolve the /sports vs /health/workouts duplication:\n   - Compare /sports/activities-summary.csv against /health/workouts/workouts_summary.csv.\n   - If redundant: delete /sports/activities-summary.csv.\n   - If not: collapse /sports/ into /health/workouts/.\n\n2. Apply health reorg per meta/ia-spec.md target tree using git mv only:\n   - health/tracking/ (sleep.json, heart_rate.json, activity.json)\n   - health/workouts/ (full, summary, trackpoints)\n   - health/vitals/blood-pressure/\n   - health/medical/\n\nDo NOT touch schema.json or import scripts in this session — that's S03b.\n\nVerification: git status shows only expected moves. Tree matches ia-spec.md for health/ paths.\n\n---END S03a---","S03b":"STANDING INSTRUCTIONS\n\nBefore starting: git checkout main && git pull origin main in pcblueprint-archive.\nThen: git checkout -b claude/s03b-schema-scripts.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Append an entry to pcblueprint-archive/sessions/session-log.en.md AND session-log.pt.md: date, session ID, repos touched, what changed, follow-ups.\n- Push branch: git push -u origin claude/s03b-schema-scripts.\n- Open PR titled \"S03b — schema.json + Import Scripts + Manifest\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S03b — schema.json + Import Scripts + Manifest — pcblueprint-archive — ~40 min\n\nChanges:\n\n1. Update schema.json paths to match the new tree from S03a.\n2. Update scripts/import-coros-fit.ts output paths.\n3. Update scripts/import-omron-csv.ts output paths.\n4. Update meta/part2-manifest.json.\n\nVerification: pnpm tsc --noEmit passes. If fixtures exist, do a dry-run of one import script and confirm it writes to the new paths.\n\n---END S03b---","S04a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S03b is merged on pcblueprint-archive/main.\n- Pull pcblueprint-archive main locally.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s04a-path-constants.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s04a-path-constants.\n- Open PR titled \"S04a — Path Constants + fileService + Fixtures\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S04a — Path Constants + fileService + Fixtures — pcblueprint-api — ~45 min\n\nChanges:\n\n1. Update ARCHIVE_PATH-derived constants and helpers in src/services/fileService.ts.\n2. Update vitest fixtures to match the new tree.\n3. Do NOT touch route files in this session — those are S04b and S04c.\n\nAcceptable: pnpm test may fail at route level after this session. That's expected; route updates come next.\n\nVerification: pnpm tsc --noEmit passes. Constants reference correct new paths. Fixtures match.\n\n---END S04a---","S04b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S04a is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s04b-health-routes.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s04b-health-routes.\n- Open PR titled \"S04b — Health Routes Realign\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit. IMPORTANT: commit per route file.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S04b — Health Routes Realign — pcblueprint-api — ~40 min\n\nScope: src/routes/health/* ONLY. Do not touch biography/, blueprint.ts, or identity/* — those are S04c.\n\nChanges:\n\n1. Walk every file in src/routes/health/*.\n2. Fix every read/write path to match the new archive tree.\n3. Commit per route file (not one mega-commit).\n\nVerification: pnpm test -- health passes green. Other tests may still fail — ignore for now; S04c fixes them.\n\n---END S04b---","S04c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S04b is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s04c-bio-routes.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s04c-bio-routes.\n- Open PR titled \"S04c — Biography/Blueprint/Identity Routes\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit. IMPORTANT: commit per route file.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S04c — Biography/Blueprint/Identity Routes — pcblueprint-api — ~40 min\n\nScope: src/routes/biography/*, src/routes/blueprint.ts, src/routes/identity/*.\n\nChanges:\n\n1. Walk every file in those three areas.\n2. Fix every read/write path to match the new archive tree.\n3. Commit per route file.\n\nVerification: pnpm test passes fully green (all route domains now aligned).\n\n---END S04c---","S05a":"✅ DONE — PR #16 merged 2026-04-24 (commit 107beb6)\n\nShipped:\n  - POST /biography/memories              — create memories/<slug>.md with optional YAML frontmatter for `name`\n  - PATCH /biography/memories/:slug       — update name and/or body_markdown; preserves unsupplied field\n  - DELETE /biography/memories/:slug      — remove the file\n  - PATCH /blueprint/:version             — replace content; sets identity_stale: true\n  - DELETE /blueprint/:version            — remove file + prune meta.versions; if deleted version was latest_version, picks previous as new latest and sets identity_stale: true\n  - Added fileExists + deleteFile helpers to fileService.ts.\n\nTests: 63/63 passing (added 23 new cases across memories.test.ts + blueprint.test.ts). No package-lock changes (repo has pre-existing drift; reverting on install per standing rule).\n\nPR: https://github.com/apcanario/pcblueprint-api/pull/16\n\n---\n\nSTANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S04c is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s05a-memories-blueprint-crud.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s05a-memories-blueprint-crud.\n- Open PR titled \"S05a — Memories CRUD + Blueprint PATCH/DELETE\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S05a — Memories CRUD + Blueprint PATCH/DELETE — pcblueprint-api — ~45 min\n\nNew endpoints:\n\n1. /biography/memories:\n   - POST\n   - PATCH /:slug\n   - DELETE /:slug\n   (operates on individual memory markdown files)\n\n2. /blueprint/:version:\n   - PATCH\n   - DELETE\n   (existing GET/POST stay as-is)\n\nRequirements:\n- Zod schema per entity.\n- Every write goes through gitService.commit().\n- Vitest coverage per endpoint: happy path + one validation failure.\n\nVerification: pnpm test green. A local curl to each new endpoint lands a git commit.\n\n---END S05a---","S05b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05a is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s05b-sessions-crud.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s05b-sessions-crud.\n- Open PR titled \"S05b — Sessions CRUD\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S05b — Sessions CRUD — pcblueprint-api — ~40 min\n\nNew endpoints on /biography/sessions:\n- GET (list)\n- GET /:id\n- POST (append session log entry)\n\nRequirements:\n- Zod schema.\n- Writes via gitService.commit().\n- Vitest coverage: happy path + one validation failure.\n\nVerification: pnpm test green. curl returns list and reads a single session.\n\n---END S05b---","S05c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05b is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s05c-chronology-crud.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s05c-chronology-crud.\n- Open PR titled \"S05c — Chronology CRUD\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S05c — Chronology CRUD — pcblueprint-api — ~40 min\n\nNew endpoints on /biography/chronology/events:\n- POST\n- PATCH /:id\n- DELETE /:id\n\nRequirements:\n- Zod schema.\n- Writes via gitService.commit().\n- Vitest coverage: happy path + one validation failure.\n\nVerification: pnpm test green. curl each endpoint; commit lands.\n\n---END S05c---","S06a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05c is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s06a-archive-bidir-sync.\n\nNOTE: Touches pcblueprint-api (container-side behavior). No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to document the new sync behavior + env vars.\n- Update CLAUDE.md if conventions changed.\n- Push branch + open PR titled \"S06a — Archive Bidirectional Sync\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06a — Archive Bidirectional Sync — pcblueprint-api — ~45 min\n\nBackground: the 2026-04-25 reconciliation confirmed api → GitHub push works end-to-end via commitArchive(), but nothing pulls GitHub → NAS. A laptop-side push to pcblueprint-archive leaves the NAS bind mount stale, and the next api write commits on top of divergent history — push fails silently (gitService warns, doesn't throw). Also investigate: /volume2/docker/git-clone2/ DSM project was supposed to handle scheduled pulls but the archive reflog only shows the initial clone — it either never ran or has been failing.\n\nGoal: periodic git pull --rebase origin main inside the api container every 10 min. On conflict, log a structured ERROR and optionally POST to ALERT_WEBHOOK_URL if set; leave the repo in a state the human can inspect and resolve.\n\nImplementation:\n- Add node-cron dependency (runtime).\n- New src/services/gitSync.ts with pullArchive() and startArchivePuller(). pullArchive tries fetch + fast-forward first; on non-FF tries rebase; on rebase conflict aborts rebase and alerts.\n- Hook startArchivePuller() in server.ts after app.listen; skip when NODE_ENV=test.\n- Harden commitArchive() in gitService.ts: on push failure, inspect stderr — if non-FF, run pullArchive + retry push once. Still throws nothing but logs ERROR + alerts on the second failure.\n- Vitest coverage: mock spawnSync; three cases — happy pull, already-up-to-date pull, conflict pull (verify alert fn called).\n\nIf git-clone2 DSM project is still scheduled, decide in PR whether to deprecate (container-side cron supersedes) or keep for off-hours safety-net.\n\nVerification:\n- pnpm test green.\n- Post-merge smoke: push a trivial commit to pcblueprint-archive from laptop; wait 10 min; docker exec into api, verify git log shows the commit.\n\n---END S06a---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S06b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05c merged (and S06a if already landed).\n- In pcblueprint-sync: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s06b-sync-deploy.\n\nNOTE: Touches pcblueprint-sync (new GHA workflow) + /volume2/docker/api/compose.yaml on the NAS (Watchtower list) + new /volume2/docker/sync/ directory on the NAS (compose + env). No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Verify the sync README Setup section still matches what was actually deployed; reconcile if not.\n- Update CLAUDE.md if conventions changed.\n- Push branch + open PR titled \"S06b — Sync Worker Containerise + Deploy\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06b — Sync Worker Containerise + Deploy — pcblueprint-sync + NAS — ~45 min\n\nBackground: the pcblueprint-sync README was rewritten on 2026-04-25 to describe image-based deployment via ghcr.io + Watchtower. But the plumbing behind that deploy path doesn't exist yet — the README describes a target state, not current reality. Three pieces must be built:\n\n1. .github/workflows/publish.yml in pcblueprint-sync — builds + pushes image to ghcr.io on merge to main. Mirror pcblueprint-api's workflow exactly (Node 22 matrix, GHCR creds from GITHUB_TOKEN, tag :latest + :sha).\n2. Watchtower must watch the new container — append pcblueprint-sync to the command: list of the Watchtower service in /volume2/docker/api/compose.yaml.\n3. First-time deploy on NAS — create /volume2/docker/sync/compose.yaml (exactly as in the sync README Setup), create /volume2/docker/sync/.env (0600) with all credentials, then docker compose up -d.\n\nOrdering matters: workflow must land + run + push the first image to ghcr.io before the NAS compose can pull it. So: step 1 → merge + wait for first image → steps 2+3.\n\nCredentials needed in .env (reference by name only):\n- API_URL=http://pcblueprint-api:3001\n- API_TOKEN (same as API_KEY in /volume2/docker/api/compose.yaml)\n- ZEPP_EMAIL, ZEPP_PASSWORD, ZEPP_REGION\n- STRAVA_CLIENT_ID, STRAVA_CLIENT_SECRET, STRAVA_REFRESH_TOKEN\n- ALERT_WEBHOOK_URL (optional)\n\nVerification:\n- docker logs pcblueprint-sync shows successful boot + scheduled cron registration.\n- On next cron tick (03:00 Strava, 11:00 Zepp), logs show a successful POST to api.\n- Watch the archive's sessions/session-log for a new API update commit triggered by the ingest write.\n\n---END S06b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S06c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- No code branch unless credential-helper work is added to api. If it is: pcblueprint-api + claude/s06c-infra-hardening.\n- Otherwise a pure ops session — just DSM work.\n\nNOTE: Primarily NAS-side. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Document the Task Scheduler script + credential-helper approach in pcblueprint-api/CLAUDE.md (under User Environment).\n- If code changed, push branch + open PR titled \"S06c — Prod Infra Hardening\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06c — Prod Infra Hardening — ops / NAS — ~30 min\n\nTwo small, high-value infra fixes surfaced during the 2026-04-25 reconciliation.\n\n1. Docker socket chgrp persistence.\nToday apcanario can use the docker CLI only because of a one-off sudo chgrp docker /var/run/docker.sock && sudo chmod g+rw ... NAS reboot reverts this. Fix via DSM Task Scheduler:\n- Control Panel → Task Scheduler → Create → Triggered Task → User-defined script\n- Event: Boot-up. User: root.\n- Script:\n    chgrp docker /var/run/docker.sock\n    chmod g+rw /var/run/docker.sock\n- Verify: reboot the NAS, SSH in as apcanario, run docker ps — succeeds without further sudo.\n\n2. GitHub PAT stored out-of-URL.\nToday the PAT is baked into /data/archive/.git/config's remote URL. Rotating the PAT means manually rewriting that file. Clean fix options (pick one):\na. Docker secret + git credential helper. Add GITHUB_PAT to /volume2/docker/api/.env (0600). In docker-entrypoint.sh, write it to a helper file (e.g. ~/.git-credentials) and run git config --system credential.helper store. Change the remote URL back to plain https://github.com/apcanario/pcblueprint-archive.git.\nb. Keep the embedded token but document a one-liner rotation procedure in CLAUDE.md (docker exec pcblueprint-api git -C /data/archive remote set-url origin https://x-access-token:NEW_PAT@github.com/apcanario/pcblueprint-archive.git).\n\nOption (a) is cleaner long-term; (b) is 5 minutes. Your call.\n\nVerification:\n- Reboot the NAS (or stop + start the api container). Confirm apcanario still has docker access and that commitArchive() still pushes successfully (trigger via any POST to an api endpoint that writes).\n\n---END S06c---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S06d":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05c merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s06d-memoir-cleanup.\n\nNOTE: No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if route table shrinks.\n- Push branch + open PR titled \"S06d — Memoir Endpoints Cleanup\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06d — Memoir Endpoints Cleanup — pcblueprint-api — ~30 min\n\nDecision point. Four GET endpoints currently 404 because their source files live only in _legacy/2026-04-16/ (intentionally deprecated — Pedro has since restructured the memoir narrative and does not want the old content promoted):\n\n- GET /biography/memories/book              → writing/memories/1934 to now, a Story of Remembrance.md (file absent)\n- GET /biography/memories/athletic-record   → writing/memories/athletic-record.md (file absent)\n- GET /biography/memories/family-book       → writing/memories/family-book-transcription.md (file absent)\n- GET /biography/memories/reconstruction    → writing/memories/reconstruction-2023-2026.md (file absent)\n\nOptions per endpoint (ask before bulk-applying):\n\nA. Remove the endpoint. Delete the route handler in routes/biography/memories.ts, any README/CLAUDE.md reference, the matching PATHS constant in fileService.ts (or wipFiles entry in search.ts). Remove related tests.\n\nB. Keep the route; leave PATHS pointing at the expected future location. Endpoint 404s until you recreate the content there. Defer further decisions.\n\nC. Bulk remove all four. Cleanest if you've fully moved on from that memoir shape.\n\nThis session is mostly decision-capture + surgical delete. Expect to touch: routes/biography/memories.ts, routes/search.ts (wipFiles + book indexer), src/services/fileService.ts (PATHS), README.md route table, tests (if any were specific to these endpoints).\n\nVerification:\n- pnpm test green after removals.\n- GET on any removed path hits the express 404 catch-all (not a route handler returning 404).\n- README route table matches actual live routes.\n\n---END S06d---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S06e":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05c is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s06e-manifest.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to document /archive/manifest as a public contract.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s06e-manifest.\n- Open PR titled \"S06a — Archive Manifest Endpoint\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06a — Archive Manifest Endpoint — pcblueprint-api — ~40 min\n\nNew endpoint: GET /archive/manifest.\n\nBehavior:\n- Reads pcblueprint-archive/meta/ia-spec.md (or derived meta/ia.json) at boot.\n- Caches in memory.\n- Returns:\n    {\n      sections: [\n        { slug, label, description, routes, counts }\n      ],\n      generated_at\n    }\n\nDocument the shape in README.md as a public contract — the website binds to it.\n\nVerification: pnpm test green. curl GET /archive/manifest returns tree matching ia-spec.md.\n\n---END S06a---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S06f":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S03b is merged on pcblueprint-archive/main.\n- In pcblueprint-sync: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s06f-sync-audit.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md with verified paths.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s06f-sync-audit.\n- Open PR titled \"S06b — Sync Path Audit\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S06b — Sync Path Audit — pcblueprint-sync — ~30 min\n\nAudit every write path in src/**.\n\nFor each write path:\n- If it was changed by S03a/S03b, update the code.\n- If it's still correct, leave the code alone but document it in README.\n\nVerification: dry-run one sync job if supported; files land in correct new paths.\n\n---END S06b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S10a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S05c is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s10a-chat-endpoint.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s10a-chat-endpoint.\n- Open PR titled \"S10a — Chat Provider Adapter + /chat Endpoint\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S10a — Chat Provider Adapter + /chat Endpoint — pcblueprint-api — ~40 min\n\nChanges:\n\n1. src/chat/providers/index.ts — LLMProvider interface with generate(messages, tools): AsyncIterable<Event>.\n2. src/chat/providers/stub.ts — deterministic stub provider. NO tool emissions yet.\n3. POST /chat — streaming endpoint (SSE or chunked). Body { messages, session_id }. Streams assistant text only in this session.\n4. .env: CHAT_PROVIDER=stub|claude|openai|…. Default to stub.\n5. Reuse existing bearer-token middleware.\n\nDo NOT implement tools.ts yet — that's S10b.\n\nVerification: pnpm test green. curl POST /chat with a plain message streams back assistant text.\n\n---END S10a---\n\nARCHITECTURE PIVOT (2026-04-25): The chat is NOT a conversational chatbot. It is a stateless natural-language task endpoint with three verbs: WRITE / QUERY / EXPORT. Each interaction is one task, no conversation history retained between requests.\n\nAdjustments to the original implementation:\n- Request body drops `session_id`. New shape: `{ messages: [<single user turn>], file?: <multipart>, intent?: <optional hint> }`.\n- No state persisted between requests on the server.\n- The `messages` array passed into the provider is the agent's INTERNAL tool loop within ONE transaction — it is NOT cross-request chat history.\n- Response: stream events until the final assistant turn, then close. No \"continue conversation\" semantics.\n\nSee memory `chat_architecture_three_verbs.md` for the full WRITE/QUERY/EXPORT contracts.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S10b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S10a is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s10b-write-tools.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s10b-write-tools.\n- Open PR titled \"S10b — Write Tools (log_bp + create_journal)\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S10b — Write Tools (log_bp + create_journal) — pcblueprint-api — ~45 min\n\nChanges:\n\n1. Create src/chat/tools.ts with two tools:\n   - log_blood_pressure(systolic, diastolic, pulse, arm, notes?) → existing BP write path.\n   - create_journal_entry(title, body, location?, date?) → journal write; mirror to memories/ if tagged.\n\n2. Teach the stub provider (src/chat/providers/stub.ts) to emit these tool_calls deterministically:\n   - \"log bp 120 80 pulse 60 left arm\" → log_blood_pressure tool_call\n   - \"new journal entry titled X: body Y\" → create_journal_entry tool_call\n\n3. Vitest: full round-trip tests — stub emits tool_call, tool executes via existing route, archive commit lands.\n\nVerification: pnpm test green. curl POST /chat \"log bp 118/76 pulse 62 left arm\" streams tool_call and writes a BP record.\n\n---END S10b---\n\nARCHITECTURE PIVOT (2026-04-25): Every write tool MUST return a standardised response shape:\n  `{ saved_path: string, github_url: string, viewer_url: string, tags_applied: string[] }`\nThe frontend (S11c) renders these as clickable links so Pedro can verify writes landed. log_bp + create_journal set the pattern; every later write tool inherits it.\n\nIdentity-based duplicate detection contract (applies to every write tool):\n- Define `identityOf(record)` for the domain (e.g. journal: `(date, body_hash)`; BP: `(date, time, arm)`).\n- Implement `findExisting(identity)` against the relevant archive file(s).\n- Three-way decision on every write:\n  1. All-new → write all, normal confirm.\n  2. Partial overlap → silently dedupe, write only the delta, surface a breakdown (\"added X, skipped Y already in archive\").\n  3. Full duplicate → escalate to user with side-by-side comparison + 3-way choice [Keep existing / Replace / Keep both]. \"Keep both\" = versioned suffix (`-v2`, `-v3`).\n\nMove shared dedup logic into `src/chat/tools/dedup.ts` (or similar) so every write tool consumes the same utility.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S10c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S10b is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s10c-read-tools.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s10c-read-tools.\n- Open PR titled \"S10c — Read Tools (query_health + query_journal)\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S10c — Read Tools (query_health + query_journal) — pcblueprint-api — ~40 min\n\nChanges:\n\n1. Add two tools to src/chat/tools.ts:\n   - query_health_window(start, end, metrics: ['sleep'|'hr'|'hrv'|'activity'|'bp']) → structured JSON.\n   - query_journal(since, tags?, query?) → matching entries.\n\n2. Stub provider emits these tool_calls on canned phrases:\n   - \"how did I sleep last week\" → query_health_window\n   - \"show me my journal entries from X\" → query_journal\n\n3. Vitest: each tool returns well-formed JSON matching its schema.\n\nVerification: pnpm test green. curl POST /chat with canned phrases streams the right tool_call + data.\n\n---END S10c---\n\nARCHITECTURE PIVOT (2026-04-25): Add `get_tag_vocabulary()` to the read-tool catalog. Reads `writing/identity/tags` and returns the canonical tag list. Required by every WRITE flow — the agent applies only canonical tags, never invents free-text tags (which would fragment future search).\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S10d":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S10c is merged on pcblueprint-api/main.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s10d-analyze-wellbeing.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s10d-analyze-wellbeing.\n- Open PR titled \"S10d — analyze_wellbeing Orchestrator\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S10d — analyze_wellbeing Orchestrator — pcblueprint-api — ~35 min\n\nChanges:\n\n1. Add analyze_wellbeing(window_days) to src/chat/tools.ts. It bundles journal + sleep + HRV + HR for the window into one payload.\n2. Stub provider emits analyze_wellbeing on \"analyze my last N days\" style phrases.\n3. Vitest: returns well-formed bundled payload.\n\nVerification: pnpm test green. curl POST /chat \"analyze my last 7 days\" returns a bundled JSON payload via tool_call.\n\n---END S10d---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S11a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S07b is merged on pcblueprint-website/main.\n- Confirm S10a is merged on pcblueprint-api/main.\n- Start pcblueprint-api running locally.\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s11a-streaming-client.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s11a-streaming-client.\n- Open PR titled \"S11a — Streaming Client + Markdown + Persistence\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S11a — Streaming Client + Markdown + Persistence — pcblueprint-website — ~45 min\n\nChanges:\n\n1. Replace the stub components/ChatPanel.tsx with a real streaming client that hits /api/chat (which proxies to backend POST /chat).\n2. Markdown rendering for assistant prose — reuse existing MarkdownRenderer.\n3. localStorage persistence: chat history keyed per day. \"New chat\" button clears current day.\n4. Update app/api/chat/route.ts to proxy real backend + forward bearer token server-side.\n5. Tool events render as RAW JSON blocks in this session — ToolCallCard comes in S11b.\n\nVerification: pnpm build green. Plain chat messages stream in; history persists across refresh; \"New chat\" clears it.\n\n---END S11a---\n\nARCHITECTURE PIVOT (2026-04-25): The chat is stateless. DROP the persistence half of this session. The frontend is a \"command bar + result panel + deliverable-download slot\" — NOT a chat thread.\n\nAdjustments:\n- No conversation thread UI. Each task = one round-trip; after the result, input clears for the next task.\n- Optional in-memory session-only history strip showing the last few tasks for context (no persistence, no cross-device sync, no sqlite).\n- Streaming text rendering still applies WITHIN a single task (agent emits partial text + tool calls before final answer).\n- Markdown rendering still applies for answers.\n- Add a deliverable-download slot in the result panel for EXPORT verb outputs.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S11b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S11a is merged on pcblueprint-website/main.\n- Confirm S10c is merged on pcblueprint-api/main.\n- Start pcblueprint-api running locally.\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s11b-tool-cards-read.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s11b-tool-cards-read.\n- Open PR titled \"S11b — ToolCallCard + Read Result Rendering\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S11b — ToolCallCard + Read Result Rendering — pcblueprint-website — ~45 min\n\nChanges:\n\n1. New components/ToolCallCard.tsx — inline card per tool call.\n2. Render READ tools inline inside the card:\n   - query_health_window → mini sleep/HR chart (reuse existing chart components).\n   - query_journal → list of entries with titles and snippets.\n   - analyze_wellbeing → summary card with key metrics.\n3. Do NOT wire WRITE tools yet — that's S11c. Write tools should still fall back to raw JSON or a \"not yet implemented\" state.\n\nVerification: send \"how did I sleep last week\" → inline sleep chart renders inside the tool card.\n\n---END S11b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S11c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S11b is merged on pcblueprint-website/main.\n- Confirm S10b is merged on pcblueprint-api/main.\n- Start pcblueprint-api running locally.\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s11c-write-confirm.\n\nNOTE: This session does not touch pcblueprint-archive. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md to reflect any changes.\n- Update CLAUDE.md if conventions, paths, or contracts changed.\n- Push branch: git push -u origin claude/s11c-write-confirm.\n- Open PR titled \"S11c — Write Tool Confirm Flow\" with short body.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S11c — Write Tool Confirm Flow — pcblueprint-website — ~40 min\n\nChanges:\n\n1. Extend components/ToolCallCard.tsx: for WRITE tools (log_blood_pressure, create_journal_entry), render Confirm / Cancel buttons.\n2. On Confirm: execute via the existing API proxy routes. Show success state after the write lands.\n3. On Cancel: dismiss the card without side effects.\n\nVerification: send \"log bp 120 80 pulse 60 left arm\" → tool card with Confirm/Cancel → Confirm → BP record lands in archive (verify via GET /health/blood-pressure) → success state shown.\n\n---END S11c---\n\nARCHITECTURE PIVOT (2026-04-25): The confirm flow is much richer than yes/no. Per the WRITE contract:\n\n1. Render extracted fields as an EDITABLE PREVIEW per domain:\n   - Lab values table (medical / blood tests)\n   - Markdown editor (journal / memory)\n   - Metadata form (chronology events, BP readings)\n2. Surface dedup outcomes:\n   - Partial overlap: breakdown (\"added X new, skipped Y already in archive\").\n   - Full duplicate: side-by-side existing vs incoming + 3-way choice [Keep existing / Replace / Keep both].\n3. Multi-domain split: if a single input contains records for multiple domains, surface the proposed split with [Save split / Bundle / Edit] choice. Default to split once approved.\n4. After commit: render the response shape (`saved_path` / `github_url` / `viewer_url`) as clickable links + the `tags_applied` list.\n\nSee memory `chat_architecture_three_verbs.md` for full contracts.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S12":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm S01 through S11c are all merged.\n- In EACH of the 4 repos: git checkout main && git pull origin main.\n- Create claude/s12-docs-sweep branch in EACH repo.\n\nThis IS the session-log session. The session-log entries for S04a through S11c were deferred — they land here.\n\nBefore ending the session you MUST:\n- Refresh README.md in all 4 repos (new IA, /chat endpoint, /archive/manifest contract, any new env vars).\n- Refresh CLAUDE.md in all 4 repos with current conventions.\n- Append session-log entries to pcblueprint-archive/sessions/session-log.en.md AND .pt.md for every session S04a through S11c (one entry per session, with commit SHAs / PR URLs from the merged PRs). Entries for S00, S01, S02a, S02b, S03a, S03b should already be present.\n- Append a final S12 summary entry listing all 28+ sessions with dates and merged-PR URLs.\n- Commit and push all 4 branches.\n- Open 4 PRs, one per repo, each titled \"S12 — Cross-Repo Docs Sweep\" with short body.\n- Do not merge without my approval. All 4 should be merged together.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S12 — Cross-Repo Docs Sweep — all 4 repos — ~30 min\n\nChanges:\n\n1. Refresh each repo's README.md: new IA, chat endpoint, manifest contract, any new env vars.\n2. Refresh each repo's CLAUDE.md with current conventions.\n3. Append deferred session-log entries to pcblueprint-archive for S04a through S11c.\n4. Append final summary entry listing all sessions with dates and PR URLs.\n5. Confirm all s01–s11c feature branches are deleted (remote + local).\n6. Confirm no orphaned branches exist on any of the 4 repos.\n\nVerification: every README mentions /archive/manifest, /chat, and the new IA. Session log is complete. All 4 s12 branches are pushed as PRs. No orphaned branches.\n\n---END S12---\n\nADDITIONAL CHECK (added 2026-04-25 post-S06a): verify `ALERT_WEBHOOK_URL` is documented consistently across `pcblueprint-api/README.md`, `pcblueprint-api/CLAUDE.md`, `pcblueprint-api/.env.example`, AND `pcblueprint-sync/README.md`. S06a added it on the api side; the sync side may already reference it (Setup section) — confirm wording and required/optional status match.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S04d":"STANDING INSTRUCTIONS\n\nThis is a cross-repo session. Repeat the same flow in EACH of the 4 repos: pcblueprint-archive, pcblueprint-api, pcblueprint-website, pcblueprint-sync.\n\nBefore starting:\n- In each repo: git checkout main && git pull origin main.\n- In each repo: git checkout -b claude/s04d-operating-rules.\n\nNOTE: This session does not modify pcblueprint-archive content (only its CLAUDE.md). Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Push branch in each repo: git push -u origin claude/s04d-operating-rules.\n- Open one PR per repo titled \"S04d — CLAUDE.md Operating Rules\" with a short body referencing the post-S04b incident (assistant pasted live secret values into chat).\n- Do not merge without my approval. All 4 should be merged together.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S04d — CLAUDE.md Operating Rules (cross-repo) — all 4 repos — ~30 min\n\nGoal: codify the 3 security rules surfaced after S04b into every repo's CLAUDE.md, so all future Claude sessions inherit them automatically.\n\nChanges (in EACH of the 4 repos):\n\n1. Open the repo's CLAUDE.md.\n2. Add a new section titled \"Working with Claude — Operating Rules\" immediately after the existing intro/conventions section. Section body — verbatim:\n\n   - Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n   - When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n   - Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — pre-existing lockfile drift is being resolved separately in S04h.\n   - When realigning routes to PATHS constants (S04a/S04b pattern), update vi.mock('../services/fileService', ...) factories in the matching test file(s) to spread the actual module first (so PATHS and helpers stay defined), then override only the disk-touching fns (readJson, writeJson, readMarkdown, appendMarkdown, writeMarkdown, appendCsvRow).\n   - Docs-only pushes (changes confined to **/*.md, LICENSE, .gitignore) no longer trigger Docker rebuilds — don't wait for a deploy notification after a pure docs PR.\n\n3. Commit per repo (4 commits total, one per repo).\n\nVerification: each repo's CLAUDE.md contains the new section under a \"Working with Claude — Operating Rules\" heading. PRs open in all 4 repos.\n\n---END S04d---","S04e":"✅ DONE — 2026-04-24\n\nVerified on NAS: /volume2/pcblueprint-archive/medical/notes/ does not exist — nothing was ever written to the wrong path before the S04b fix landed. No cleanup PR needed. Session marked complete.\n\nVerification command (run again if unsure):\n  ssh apcanario@192.168.1.69 'ls -la /volume2/pcblueprint-archive/medical/notes/ 2>&1'\nExpected: \"No such file or directory\".\n\n---\n\nWHAT TO DO (MANUAL — you do this, no Claude Code needed)\n\nReason: the S04b health-routes realign uncovered a bug where medical notes were being written to medical/notes/<slug>.md (wrong tree) instead of the correct path. The bug was fixed in PR #9, but we never checked the live archive for stray files written by the buggy code before it was fixed.\n\n1. SSH into the NAS or open Task Scheduler.\n\n2. Run:\n   ls -la /volume2/pcblueprint-archive/medical/notes/ 2>/dev/null\n\n3. Interpret the result:\n   - If \"No such file or directory\" or empty → nothing to clean. Mark this session done.\n   - If files exist → for each file:\n     a. cat the file to see what it contains.\n     b. The canonical destination is /volume2/pcblueprint-archive/records/medical/notes/<slug>.md (this is where the S04b fix routes new notes via pathMedicalNote()).\n     c. git mv the file (in a working clone) to that path, commit on a small branch named prep/s04e-medical-notes-cleanup, open a PR titled \"S04e — Stray medical/notes cleanup\" against pcblueprint-archive.\n\n4. Mark this session done once the audit is complete (with or without files found).","S04f":"⏭ SKIPPED — will reassess at the end of the project (post-S12). Marked done in the runbook so it stops surfacing as the next task; safe to ignore for now.\n\nAudit done; recommendation: The orphan at /volume1/pcblueprint-archive is real (58 MB, identical to the live copy) but NOT passive. It is actively refreshed by a docker project defined at /volume2/docker/compose.yaml (service: git-clone → alpine/git → clones repo to /volume1 using a hardcoded PAT).\n\nThe live archive is at /volume2/pcblueprint-archive and is maintained by an independent, active project /volume2/docker/git-clone2/compose.yaml. Nothing on the NAS reads /volume1. So the orphan is harmless but removing it requires stopping the /volume2/docker/compose.yaml project in Container Manager first.\n\nWhy deferred: Pedro prefers to finish the project first, then clean up cosmetic drift, to avoid any chance of disrupting production during the build-out.\n\nClose-out cleanup checklist (do in this order):\n  1. Container Manager → Project → stop + delete the project whose compose.yaml is /volume2/docker/compose.yaml (NOT the `api` or `git-clone2` projects — those must stay).\n  2. Control Panel → Task Scheduler → delete the 3 user-defined diagnostic scripts (IDs 1777020656, 1777020950, 1777021069 under /volume2/docker/synoscheduler/).\n  3. ssh apcanario@192.168.1.69 'rm -rf /volume1/pcblueprint-archive'\n  4. Verify: ssh apcanario@192.168.1.69 'ls /volume1/pcblueprint-archive 2>&1' → expect \"No such file or directory\".\n\nSecurity note: the PAT in /volume2/docker/compose.yaml and /volume2/docker/git-clone2/compose.yaml is world-readable on the NAS. Threat model is low (only family accounts on the NAS), so hardening is optional. Can be moved to a 600-perm .env file in the same directory with `${GH_TOKEN}` substitution — out of scope for this session.\n\n---\n\nWHAT TO DO (MANUAL — you do this, no Claude Code needed)\n\nReason: /volume1/pcblueprint-archive is a stale leftover from the volume1→volume2 migration. The live archive is /volume2/pcblueprint-archive. Removing the orphan prevents future Claude sessions or scripts from accidentally reading or writing to the wrong volume.\n\n1. Sanity-check that /volume2/pcblueprint-archive is the active path:\n   ls /volume2/pcblueprint-archive | head\n\n2. Verify nothing references /volume1/pcblueprint-archive in your active configs/scripts:\n   - grep -r \"volume1/pcblueprint-archive\" /volume2/docker/ 2>/dev/null\n   - grep for the same string in any sync/cron scripts you maintain.\n\n3. If grep returns hits → STOP. Update those references to /volume2 first, in a small PR. Re-run grep until clean.\n\n4. If grep is clean:\n   - Take a final byte-count snapshot: du -sh /volume1/pcblueprint-archive\n   - Remove: rm -rf /volume1/pcblueprint-archive\n   - Verify removal: ls /volume1/pcblueprint-archive (expect \"No such file or directory\")\n\n5. Note completion in your own records — this is NAS housekeeping, no PR needed.\n\nADDITIONAL SCOPE (added 2026-04-25 post-S06a): also remove `/volume2/docker/git-clone2/`. Confirmed during S06a investigation that this DSM project is a one-shot `alpine/git clone` (no schedule); container exited 10 days ago after the initial clone and has never run since. The README claim that it handles scheduled pulls was incorrect — that role is now owned by the container-side cron in pcblueprint-api (`src/services/gitSync.ts` shipped in S06a). Safe to delete the entire `/volume2/docker/git-clone2/` directory.","S04g":"✅ DONE — resolved via PR #12 (merged 2026-04-24 09:42 UTC)\n\nRoot cause matched the runbook prediction:\n  1. Missing OIDC permission — workflow was missing `id-token: write`.\n  2. Deprecated input — anthropics/claude-code-action@v1 dropped `custom_instructions`.\n\nResolution: rather than fixing the workflow, it was deleted. Rationale from the PR body: \"interactive Claude Code sessions already do PR-style review as part of every task\". The \"All jobs failed\" email noise stopped; PR review is now covered by live sessions.\n\nPR: https://github.com/apcanario/pcblueprint-api/pull/12\nThe ANTHROPIC_API_KEY secret is still set in the repo (2026-04-13). If the bot is ever wanted back, re-add a workflow with `id-token: write` in permissions and `trigger_phrase` instead of `custom_instructions`.\n\n---\n\nWHAT TO DO (MANUAL — you do this, no Claude Code needed)\n\nReason: the Claude PR Review bot silently failed on PRs #9 and #10 in pcblueprint-api. Subsequent sessions will continue opening PRs and need automated review restored. The failure is likely config/secrets, not code.\n\n1. Open the GitHub Actions tab for pcblueprint-api. Find the PR Review workflow runs for PRs #9 and #10. Read the failure logs.\n\n2. Check, in order:\n   a. Repo Settings → Secrets and variables → Actions: confirm the API key secret the workflow expects is present and not expired (reference by name only, do not echo the value).\n   b. The workflow .yml file under .github/workflows/: confirm the trigger matches your PR style (pull_request.types, branches filter, etc.) and that referenced secrets/env match step 2a.\n   c. The Claude GitHub App (or bot account) under Settings → Integrations: confirm it still has access to the repo and the right permissions.\n   d. If using anthropics/claude-code-action or similar: check the action's recent releases for breaking changes.\n\n3. Once root cause is found:\n   - If it's the workflow file: open a small PR titled \"S04g — Fix Claude PR Review bot\" on pcblueprint-api with the fix.\n   - If it's a secret/permission: rotate/grant via GitHub UI, no PR needed. Document the change in pcblueprint-archive/sessions/session-log.en.md and .pt.md so future sessions know.\n\n4. Re-trigger the workflow on PR #9 (or any open PR) and confirm a review is posted before marking this session done.","S04h":"STANDING INSTRUCTIONS\n\nBefore starting:\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s04h-lockfile-drift.\n\nNOTE: This session does not modify pcblueprint-archive content. Do NOT add a session-log entry here — it will be batched into S12.\n\nBefore ending the session you MUST:\n- Update the repo's README.md only if the fix changes a documented dev workflow.\n- Push branch: git push -u origin claude/s04h-lockfile-drift.\n- Open PR titled \"S04h — Resolve package-lock.json drift\" with a short body explaining the root cause and the fix.\n- Do not merge without my approval.\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR. (EXCEPTION: this session IS that dedicated PR. See SESSION body below.)\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S04h — Resolve package-lock.json drift — pcblueprint-api — ~30 min\n\nBackground: routine `npm install` runs produce a non-trivial lockfile diff even when no dependency changes are intended. This session is the dedicated PR to resolve that drift cleanly so the standing rule \"don't commit lockfile drift\" can be enforced going forward.\n\nSteps:\n\n1. Confirm clean working tree on a fresh main.\n2. Capture current versions: node -v && npm -v. Note them in the PR body.\n3. Delete node_modules/ and package-lock.json.\n4. Run npm install fresh.\n5. Inspect the resulting package-lock.json diff against main.\n6. Investigate root cause(s):\n   - Mismatched Node/npm versions (compare to .nvmrc / package.json engines field if present).\n   - Range-pinned dependencies upgrading transitively.\n   - Optional / platform-specific dependencies.\n7. If a clear fix is available (e.g. add .nvmrc, pin engines field, narrow a range), apply the fix in addition to regenerating the lockfile.\n8. Commit in two chunks:\n   - First commit: any pinning/engines/.nvmrc fix (if applicable).\n   - Second commit: regenerated package-lock.json.\n\nVerification: in a clean clone, npm install produces zero further changes to package-lock.json. pnpm test (or npm test) passes green.\n\nNOTE: This is the one session where modifying package-lock.json is the point. The standing rule continues to apply to all OTHER sessions.\n\n---END S04h---","S13a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S05c, S06a).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s13a-upload-api.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S13a — Upload Pipeline — api side\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S13a — Upload Pipeline — api side — pcblueprint-api — ~45 min\n\nAdd multipart file upload to the api so chat (and the website) can ingest binaries (medical PDFs, photos, voice memos, IDML/Word originals) into the archive.\n\nImplementation:\n- Add multer (or formidable) middleware. Limit per request: 50MB. Allow types: pdf, png, jpg/jpeg, webp, heic, docx, idml, m4a/mp3/wav.\n- New route POST /upload/{bucket} where bucket is one of: source-files/medical, source-files/sports, source-files/writing, writing/memories/drafts, _legacy/2026-04-25 (date-stamped quarantine for unsorted).\n- Body: multipart with file + optional metadata (title, date, tags).\n- Server-side: stream-write to bind mount path; commitArchive() with a meaningful message (\"upload: <filename> → <bucket>\").\n- Path traversal guards (reject ../ in filename, sanitise to safe basename, accept user-supplied subdirectory only via a closed allowlist).\n- Vitest: happy POST writes file + commits; oversize 413; bad type 415; path traversal 400.\n\nVerification:\n- pnpm test green; tsc clean.\n- curl -F file=@test.pdf -F bucket=source-files/medical $API/upload writes the file + creates a commit on the archive.\n\n---END S13a---\n\nARCHITECTURE PIVOT (2026-04-25): Scope expands beyond just the multipart endpoint. INCLUDE in this session:\n\n- PDF text extraction (`pdf-parse` or `pdfjs-dist`).\n- Medical lab-report schema mapper: extract per-marker records `{date, lab_name, marker, value, units, range, flag}` from common Portuguese / English clinic-issued lab PDFs. Reference ranges in the PDF are FIRST-CLASS data (used by the QUERY verb's normal-range comparison).\n- Image OCR is OUT OF SCOPE here — defer to a follow-up session if/when needed.\n\nWhy expanded: the chat's WRITE verb has PDF blood-test ingestion as a stated first-class use case; deferring the parser leaves S13a half-done.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S13b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S13a, S07b, S11a).\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s13b-upload-ui.\n\nNOTE: Touches pcblueprint-website, pcblueprint-api. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S13b — Upload Pipeline — website + chat tool\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S13b — Upload Pipeline — website + chat tool — pcblueprint-website + pcblueprint-api — ~50 min\n\nTwo surfaces:\n\n1. Website upload UI:\n- Drop-zone component (mui/material) on each archive section page (/medical, /memories, /journal). Bucket auto-selected by section context.\n- Standalone /upload page for uncategorised files (lands them in _legacy/2026-04-25/).\n- Server-action proxy via app/api/upload/route.ts → backend POST /upload/{bucket}, forwarding bearer token.\n\n2. Chat tool attach_file:\n- New tool in src/chat/tools.ts: attach_file({ filename, base64, bucket, metadata? }).\n- Stub provider emits attach_file when user says \"save this file as X\" or attaches a file in the (future) message UI.\n- For S13b chat scope: tool just calls the upload endpoint; UI for in-chat file attach is a follow-up note.\n\nVerification:\n- Drag-drop on /medical writes file + shows new entry in the page list within ~3s.\n- Chat \"save this PDF as my latest blood test\" with a base64 payload writes to source-files/medical/.\n\n---END S13b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S14a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S10b).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s14a-write-tools.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S14a — Expanded Chat Write Tools\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S14a — Expanded Chat Write Tools — pcblueprint-api — ~60 min\n\nAdd write tools so chat can ingest into every domain, not just BP + journal:\n\n- append_memory({ slug?, name, body_markdown, tags? }) — writes to writing/memories/drafts/ (or recognises an existing slug).\n- log_chronology_event({ date, title, category, body, tags? }) — proxies POST /biography/chronology/events.\n- log_medical_event({ date, summary, attachments? }) — appends to records/medical/pedro_medical.md and optionally creates records/medical/notes/<slug>.md.\n- update_blueprint({ version, body_markdown }) — proxies PATCH /blueprint/:version.\n- attach_imaging({ date, modality, file_id }) — links an already-uploaded source-files/medical/* file into pedro_medical.md.\n\nEach tool teaches the stub provider one canned phrase for round-trip testability.\n\nVitest: each tool exercises its underlying route + verifies an archive commit lands.\n\nVerification: curl POST /chat with each canned phrase produces a tool_call + write.\n\n---END S14a---\n\nARCHITECTURE PIVOT (2026-04-25): Scope expands. The chat's WRITE verb covers ALL active user-data buckets in the archive (`records/`, `writing/`, `raw/`, `source-files/`, `sessions/`), NOT a curated subset. System buckets (`_legacy/`, `meta/`, `scripts/`, `claude-code-prompts/`) are explicitly off-limits to chat writes.\n\nRequired write tools to enumerate (some already exist in the api from S05*):\n- WRITING: create_journal (S10b), create_memory (default `author=Pedro Canário`), update_identity_context, add_identity_pattern.\n- RECORDS: log_bp (S10b), add_blood_test, add_medical_note, add_medical_exam, add_medication, add_medical_flag, post_blueprint_version.\n- RAW: add_raw_record (generic raw-bucket write).\n- SOURCE-FILES: add_source_file (covered by S13a/b).\n- SESSIONS: append_session_log (already exists from S05b).\n- TAGS: add_tag_to_vocabulary.\n- CHRONOLOGY: add_chronology_event (already from S05c).\n\nLikely api gaps to fill in this session (verify + implement if missing):\n- `PATCH /identity/context`\n- `POST /identity/patterns`\n- `POST /identity/tags`\n- Verify medical sub-resources (`/health/medical/notes`, `/exams`, `/medications`, `/flags`) all have writes.\n- `POST /raw/...` for the generic raw write, if used by chat.\n\nEVERY write tool inherits the contracts from S10b:\n- Response shape `{ saved_path, github_url, viewer_url, tags_applied }`.\n- Identity-based dedup (3-way: all-new / partial / full duplicate).\n- Auto-tag from canonical vocabulary (`get_tag_vocabulary` from S10c).\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S14b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S10c, S14a).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s14b-read-tools.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S14b — Expanded Read Tools + Cross-Domain Orchestrators\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S14b — Expanded Read Tools + Cross-Domain Orchestrators — pcblueprint-api — ~75 min\n\nAdd read tools so chat can answer cross-domain queries like \"give me dad's memories where I'm mentioned\" or \"any chronology events near my hospitalization\":\n\nRead tools:\n- query_chronology({ from?, to?, category?, contains? }) — filters chronology events.\n- query_memories({ author?, contains?, tags?, since? }) — text search across writing/memories/* and writing/memories/drafts/*.\n- query_blueprint({ version? }) — fetches blueprint version body + meta.\n- query_identity() — returns context + pattern registry.\n- query_medical({ contains?, since? }) — section-level search inside pedro_medical.md and notes.\n\nOrchestrators (alongside existing analyze_wellbeing):\n- cross_reference({ query, domains?, window? }) — single search across ANY combination of domains, returns grouped hits with snippets. Reuses the existing /search route + adds an LLM-friendly response shape (small, clipped passages with source slugs).\n- timeline_summary({ from, to }) — bundles chronology + journal + sessions + medical + sports for a date range, deduped + chronologically merged.\n\nStub provider learns one canned phrase per tool. Vitest: each tool returns well-formed JSON matching its schema.\n\nVerification: curl POST /chat \"any of dad's memories that mention me\" → tool_call cross_reference({ query: 'Pedro', domains: ['memories'], filter: { author: 'francisco-canario' } }) → matching passages.\n\n---END S14b---\n\nARCHITECTURE PIVOT (2026-04-25): In addition to cross-domain orchestrators, ADD `web_search(query)` to the tool catalog. Required by the QUERY verb when comparing fresh inputs (blood tests, etc.) against medical reference values not embedded in the source PDFs. The agent's preference order: PDF-embedded ranges first → web_search fallback. The answer cites the source of the comparison range.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S14c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S14b at minimum).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s14c-export-tools.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S14c — Export Tools\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S14c — Export Tools — pcblueprint-api — ~40 min\n\nBackground: the chat's EXPORT verb (per the 2026-04-25 architecture pivot) needs tools to generate downloadable deliverables. See memory `chat_architecture_three_verbs.md`.\n\nImplement (each as an internal chat tool, callable by the agent):\n1. `export_to_pdf({ query, scope, options? })` — simple PDF (header: Pedro's name + date + scope; body: content; page numbers; footer). Library: `pdfkit` or `puppeteer`. Default minimal template; iterate when needed.\n2. `export_to_csv({ query, scope })` — flat CSV, descriptive column names, ISO dates. Optimised for storage / downstream tooling, NOT direct human reading.\n3. `export_to_markdown({ query, scope })` — concatenated Markdown for \"give me everything as one printable doc\" exports.\n4. `bundle_source_files({ query })` — `.zip` of selected source files (per-domain subdirs inside the zip). Universal cross-platform compatibility — never `.tar.gz`.\n5. `generate_report({ template, scope })` — composite: data + simple chart(s) + commentary, rendered to PDF.\n\nFilename convention (universal): `<verb>-<scope>-<YYYY-MM-DD>.<ext>`. Examples: `report-hrv-2026-04.pdf`, `export-journal-2024-01_to_2024-12.md`, `bundle-medical-2024.zip`.\n\nStorage: deliverables written to `<archive>/exports/<YYYY-MM>/` — does NOT pollute user-data buckets. Add a TTL cleanup script in `scripts/` (e.g. delete deliverables older than 30 days). Response includes a download URL.\n\nTool catalog: register the five tools in the existing tool surface (per S14a's enumeration). Agent picks based on user intent (\"give me a PDF report\" vs \"let me download my 2024 medical files\").\n\nVerification: `pnpm test` green. Manual smoke: ask the agent for each export type, verify the file exists at the returned URL with the expected filename + reasonable content.\n\n---END S14c---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S15a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S10a, S14a, S14b).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s15a-anthropic.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S15a — Real Chat Provider — Anthropic SDK + System Prompt\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S15a — Real Chat Provider — Anthropic SDK + System Prompt — pcblueprint-api — ~60 min\n\nReplace the stub chat provider with a real LLM. Pick Anthropic Claude (Sonnet 4.6 default; allow override via env).\n\nImplementation:\n- npm i @anthropic-ai/sdk.\n- src/chat/providers/anthropic.ts — implements LLMProvider.generate(messages, tools). Streams content_block_delta + tool_use events as the existing AsyncIterable<Event> shape.\n- Wire env: CHAT_PROVIDER=anthropic|stub|openai (still defaulted to stub for tests). ANTHROPIC_API_KEY required when CHAT_PROVIDER=anthropic.\n- System prompt that gives Claude the archive shape: bucket structure, available tools, when to use which, how to cite sources (slugs/URLs).\n- Tool registration: convert the existing JSON schemas in tools.ts into the Anthropic tool_use schema.\n\nCost guardrails:\n- Default max_tokens 1024, override via request body.\n- Budget alert at first request that costs > $0.10 (log warning).\n\nVitest: stub-mode passes; anthropic-mode requires manual smoke (no API key in CI).\n\nVerification: with ANTHROPIC_API_KEY set locally, curl POST /chat \"find me family memories from before 1995\" streams real assistant prose + a real tool_call.\n\n---END S15a---\n\nARCHITECTURE PIVOT (2026-04-25): The system prompt is NOT conversational. It encodes a stateless task router with three verbs.\n\nSystem prompt structure:\n1. Classify intent: WRITE / QUERY / EXPORT.\n2. WRITE protocol:\n   - Parse + extract records from input (text or file).\n   - Detect multi-domain input → propose split before commit.\n   - Call `get_tag_vocabulary()` to get canonical tags.\n   - Run identity-based dedup → all-new / partial / full duplicate.\n   - Surface confirmation preview (S11c) before commit.\n   - Return response with `saved_path / github_url / viewer_url / tags_applied`.\n3. QUERY protocol:\n   - Read relevant archive files via read tools.\n   - Cross-reference if needed: when comparing fresh input to history, AUTO-SAVE the input first (dedup contract still applies).\n   - For reference ranges: prefer PDF-embedded ranges, fall back to `web_search`.\n   - Always cite sources in the answer (link to the archive files read).\n   - Short structured answer for fact queries; prose for interpretive queries.\n4. EXPORT protocol:\n   - Gather data via read tools.\n   - Generate deliverable using S14c's export tools (pdf / csv / markdown / zip) following the standard filename convention `<verb>-<scope>-<YYYY-MM-DD>.<ext>`.\n   - Return download URL.\n\nProvider: Anthropic SDK with prepaid credits + workspace spend cap. Default model = Claude Haiku 4.5 (cheap + fast for simple tasks). Escalate to Sonnet 4.6 for hard cross-domain or interpretive queries (router decides per request, e.g. heuristic on query complexity / tool-call count).\n\nThree-layer cost ceiling (all in this session):\n- Per-request: `max_tokens` cap, max-iterations cap (≤10 tool calls per request), hard timeout (~60s).\n- App-level monthly budget guard: track tokens spent in a small JSON / sqlite ledger; refuse new `/chat` with 429 when 100% of monthly cap hit, warn at 80%.\n- Vendor-level: prepaid credits + workspace spend limit (set in Anthropic console, outside the codebase).\n\nAdd S14c as a prereq (was `[\"S10a\", \"S14a\", \"S14b\"]`, now `[\"S10a\", \"S14a\", \"S14b\", \"S14c\"]`) so the export tools exist when the system prompt is written.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S15b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S15a).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s15b-conversations.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S15b — Server-Side Conversation Persistence\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S15b — Server-Side Conversation Persistence — pcblueprint-api — ~45 min\n\nMove chat history off localStorage onto the server so it survives across devices (you'll be on phone + laptop).\n\nImplementation:\n- Add better-sqlite3 (single-user, single-process — sqlite is fine; no Postgres yet).\n- Schema: conversations(id, started_at, title), messages(id, conversation_id, role, content, tool_calls, created_at).\n- Routes: GET /chat/conversations, GET /chat/conversations/:id, POST /chat/conversations, DELETE /chat/conversations/:id. Existing POST /chat takes optional conversation_id; appends messages as it streams.\n- DB file at /data/archive/.chat/conversations.db (lives outside the git tree via .gitignore, keeps it on the bind mount but not committed).\n- Vitest: round-trip create → list → get → delete.\n\nVerification: send a message via curl with conversation_id; refresh; GET /chat/conversations/:id returns full history.\n\n---END S15b---\n\nARCHITECTURE PIVOT (2026-04-25): REPLACE this session's scope.\n\nOLD: \"Server-Side Conversation Persistence — sqlite-backed chat history\".\nNEW: \"Transaction Audit Log — sqlite-backed log of every chat task\" — timestamp, verb (WRITE/QUERY/EXPORT), classified intent, tools called (with args), result/error, tokens used, cost.\n\nWhy: under the new architecture, there is no conversation history to persist. But an audit trail of \"what did the agent do to my archive\" is still valuable for debugging, accountability, and answering \"what changed last Thursday?\".\n\nSchema: append-only, indexed by date + verb. Frontend may surface recent transactions in a \"recent activity\" view (out of scope for this session — backend only).\n\nTitle may want to change to `S15b — Transaction Audit Log` post-merge.\n\nSee memory `chat_architecture_three_verbs.md`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S16a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S06a, S06b).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s16a-observability.\n\nNOTE: Touches pcblueprint-api, pcblueprint-sync. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S16a — Observability — Standardized Alerting + /healthz\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S16a — Observability — Standardized Alerting + /healthz — pcblueprint-api + pcblueprint-sync — ~45 min\n\nToday the api commitArchive() warns silently on push failure; the sync worker has ALERT_WEBHOOK_URL but the api doesn't. Standardize.\n\nChanges (api):\n- New src/services/alert.ts — postAlert({ severity, source, message, details? }) → POSTs to ALERT_WEBHOOK_URL if set, always logs to stderr.\n- Wire commitArchive() to call postAlert on push/commit failure (severity=error).\n- Wire startArchivePuller() (S06a) to call postAlert on rebase conflict.\n- New GET /healthz (no auth) — returns { status: \"ok\", version, archive_head, last_commit_at }.\n\nChanges (sync):\n- Refactor existing logger.ts to share the same alert payload shape.\n\nVitest: api alert helper mocked; verify call on each error path.\n\nVerification: artificial push failure (set bad PAT) → webhook receives a JSON alert. /healthz returns expected shape.\n\n---END S16a---\n\nNOTE (added 2026-04-25 post-S06a): `pcblueprint-api/src/services/gitSync.ts` already exports `sendAlert(message)` which POSTs to `ALERT_WEBHOOK_URL`. Before adding new alerting plumbing here, extract `sendAlert` into a shared module (suggested: `pcblueprint-api/src/services/alerts.ts`) and have both the cron puller and the new `/healthz` alerting depend on it. Avoid reimplementing the webhook POST.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S16b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S06a).\n- In undefined: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s16b-backup.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S16b — Daily Off-Site Archive Backup\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S16b — Daily Off-Site Archive Backup —  — ~30 min\n\nThe archive is mirrored to GitHub via push. That's already an off-site backup, but it doesn't include the .chat/ sqlite (S15b) or other gitignored runtime state. Add a daily snapshot.\n\nImplementation:\n- DSM Task Scheduler nightly: tar -czf /volume2/backups/archive-YYYY-MM-DD.tar.gz /volume2/pcblueprint-archive (excluding .git/, since that's already mirrored).\n- Retain 14 days; rotate older.\n- Optional: rclone copy to a cloud bucket (B2 / S3 / Cloudflare R2). Requires picking a provider + credentials — defer if you don't want a paid service.\n\nNo code change required for the local snapshot. Document the schedule in pcblueprint-archive/CLAUDE.md.\n\nVerification: trigger the task once manually; confirm tarball present in /volume2/backups/.\n\n---END S16b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S19a":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S08c).\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s19a-mobile-shell.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S19a — Mobile — Audit + Sidebar Drawer\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S19a — Mobile — Audit + Sidebar Drawer — pcblueprint-website — ~60 min\n\nAudit every page on a 375px iPhone width and convert the docs-shell sidebar to a mobile drawer.\n\nImplementation:\n- Hamburger toggle on app bar < 768px.\n- Swipeable Drawer (mui/material) replaces the persistent sidebar.\n- Top nav (S07a's /today addition) collapses into the drawer.\n- Quick \"audit\" pass: list every page that visibly breaks at 375px. Capture as a checklist for S19b/c.\n\nVerification: use Chrome DevTools device toolbar (iPhone 14 Pro). Sidebar collapses; drawer opens on tap; nav is reachable.\n\n---END S19a---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S19b":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S19a, S09b).\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s19b-mobile-data.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S19b — Mobile — Charts + DataGrid Reflow\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S19b — Mobile — Charts + DataGrid Reflow — pcblueprint-website — ~45 min\n\nCharts and tables on /sports/[id] + /blood-pressure + /sleep don't fit on narrow viewports.\n\nImplementation:\n- MUI X Charts: set responsive width via container + breakpoint-aware height.\n- MUI X DataGrid: swap for a stacked-card list under 768px (extract a ResponsiveList component).\n- Sports detail hero stats: collapse to 2-col grid (was 4-col) under 600px.\n\nVerification: every chart renders without horizontal scroll on iPhone. Tables become tappable card stacks.\n\n---END S19b---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","S19c":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged (S19a, S11c).\n- In pcblueprint-website: git checkout main && git pull origin main.\n- Then: git checkout -b claude/s19c-mobile-chat.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"S19c — Mobile — Chat UX\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION S19c — Mobile — Chat UX — pcblueprint-website — ~35 min\n\nChat UI specifics on phone:\n\n- Input pinned to bottom with keyboard-aware padding (use @react-aria/utils or visualViewport API).\n- Tool call cards stack full-width; collapsible by default on mobile.\n- File-attach button inline with text input (S13b).\n- Send-on-enter on desktop, send-on-arrow-tap on mobile.\n- Long messages wrap properly; code blocks scroll horizontally inside the bubble.\n\nVerification: real conversation on phone, including a write tool with confirm.\n\n---END S19c---\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 1986 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines.\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-01":"STANDING INSTRUCTIONS\n\nBefore starting:\n- No prereqs (R1-01 starts the R1 chain — only R0 dependencies which are already shipped).\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-01-auth-core.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-01 — Auth — User Schema + Argon2id + Session Core\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-01 — Auth — User Schema + Argon2id + Session Core — pcblueprint-api — ~60 min\n\nRELEASE NOTE (2026-04-25): This session was pulled forward from the original \"Production — Auth Hardening\" phase to R1 (Private Archive Browser). Real auth gates the public archive from day one — no APP_PASSWORD-only window. Scope unchanged; implementation re-targeted from pcblueprint-website (Next.js) to pcblueprint-api (Express + EJS) on 2026-04-25 because R1 ships server-rendered HTML directly from the api. R1 RESCOPE 2026-04-25\n\nReplace the legacy single-`APP_PASSWORD` model with proper user accounts. Single-user for now (just Pedro), but built so a second user is a row insert later.\n\nImplementation in pcblueprint-api:\n- Dependencies: `pnpm add argon2 better-sqlite3 cookie-parser` (and `@types/cookie-parser` as dev dep).\n- Auth DB at `.data/auth.db` (gitignored). Schema:\n  - `users(id INTEGER PK, email TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, created_at INTEGER NOT NULL, last_login_at INTEGER, totp_secret TEXT)`\n  - `sessions(id TEXT PK, user_id INTEGER REFERENCES users(id), created_at INTEGER NOT NULL, expires_at INTEGER NOT NULL, user_agent TEXT, ip TEXT)`\n  - `password_resets(token TEXT PK, user_id INTEGER REFERENCES users(id), expires_at INTEGER NOT NULL)` — schema only; behaviour comes in R1-02.\n- Bootstrap: `scripts/seed-user.ts` reads email + password from CLI args (or stdin), inserts the user with an argon2id hash. Document one-time usage in README.\n- Service: `src/services/auth.ts` exposes `hashPassword`, `verifyPassword`, `createSession(userId, ua, ip)` returning the session id, `findValidSession(id)`, `deleteSession(id)`. Argon2id parameters: defaults from the `argon2` package are fine for v1.\n- Routes (Express, mounted before the requireAuth middleware): `POST /auth/login` (validates email+password, creates a session row, sets the cookie), `POST /auth/logout` (deletes session row, clears cookie). Cookie: `pcb_session=<sessionId>`, `HttpOnly`, `Secure` (set conditionally — false in dev, true in prod via NODE_ENV check), `SameSite=Lax`, sliding 30-day expiration. On every authenticated request that reads a valid session, refresh `expires_at` to now+30d.\n- Middleware: `src/middleware/requireAuth.ts`. Mount globally at app-level AFTER the auth routes. Bypass list: `/auth/login`, `/auth/logout`, `/login` (the EJS form route in R1-02), `/healthz` (R1-09), and any future `/auth/*` reset routes. For other routes: read the cookie, look up the session, attach `req.user` if valid. Otherwise: redirect to `/login` for HTML `Accept` headers, or return `401 { error: \"auth_required\" }` for JSON requests.\n- Tests (vitest, in `src/__tests__/`): `auth.hash.test.ts` (hash + verify roundtrip; verify rejects wrong password), `auth.session.test.ts` (createSession + findValidSession happy path + expired-session rejection), `auth.routes.test.ts` (POST /auth/login happy + wrong-pw 401 + missing-fields 400; POST /auth/logout deletes the row).\n\nOut of scope here (handled in later R1 sessions): the HTML `/login` form view (R1-02), email-based password reset (R1-02), TOTP enrollment (R1-03), rate limiting (R1-03), `/healthz` endpoint (R1-09).\n\nProduction note for later: `.data/auth.db` is fine for the NAS / Fly.io single-instance R1-08 deploy. Switching to a managed DB (Postgres / Turso / Cloudflare D1) is a future concern when scaling beyond one instance — defer entirely.\n\nVerification: `pnpm test` green. Manual smoke (Pedro runs locally):\n1. `pnpm tsx scripts/seed-user.ts <email> <password>` → users row exists.\n2. `curl -X POST http://localhost:3000/auth/login -H 'content-type: application/json' -d '{\"email\":\"...\",\"password\":\"...\"}'` → 200, `Set-Cookie: pcb_session=...` in headers.\n3. `curl -b 'pcb_session=...' http://localhost:3000/records` → 200 (or whatever the records route returns; the point is no redirect).\n4. `curl http://localhost:3000/records` (no cookie) → 302 `Location: /login` for HTML, or 401 for `Accept: application/json`.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (grep for `const COMPLETED_SESSIONS` — single source of truth, no more dual-write). Save in place — the folder is the source of truth and it lives in the `pcblueprint-meta` GitHub repo (auto-deploys to Pages on push).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-02":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm R1-01 + R1-04 merged. R1-04 lands the EJS rendering layer + base layout this session adds new views into.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-02-auth-ux.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-02 — Auth — Login Form + Password Reset (Resend)\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-02 — Auth — Login Form + Password Reset (Resend) — pcblueprint-api — ~60 min\n\nRELEASE NOTE (2026-04-25): This session was pulled forward from the original \"Production — Auth Hardening\" phase to R1 (Private Archive Browser). Real auth gates the public archive from day one — no APP_PASSWORD-only window. Scope unchanged; implementation re-targeted from pcblueprint-website (Next.js) to pcblueprint-api (Express + EJS) on 2026-04-25 because R1 ships server-rendered HTML directly from the api. R1 RESCOPE 2026-04-25\n\nAdd email-based password reset on top of R1-01's auth core, and polish R1-04's login form. Stay in pcblueprint-api / EJS — no React, no client-side framework.\n\nNote (R1-04 POST-MERGE 2026-04-25): R1-04 already shipped `src/views/login.ejs` (an email + password form posting to the existing JSON `/auth/login` via inline fetch JS) and `src/views/_layout.ejs`. This session ADDS reset templates + routes, REUSES the layout, and POLISHES the login form (drop inline JS in favour of a server-rendered POST /login + add a 'forgot your password?' link).\n\nImplementation in pcblueprint-api:\n- Dependency: `pnpm add resend`. Env: `RESEND_API_KEY`. Add an entry in README env table.\n- EJS views (under `src/views/` per R1-04's convention; R1-04 lands the rendering layer first, so this session adds new templates that fit R1-04's `_layout.ejs`):\n  - `views/reset-request.ejs` — email-only form, submits POST /auth/request-reset.\n  - `views/reset-form.ejs` — new-password form, hidden token field, submits POST /auth/complete-reset.\n  - `views/email/reset-email.ejs` — Resend email body. Plain-ish HTML, includes the reset link with the token, \"expires in 1 hour\" copy.\n- Routes (Express, all under the auth-bypass list from R1-01's middleware):\n  - `POST /login` (NEW) → calls the same handler as R1-01's `POST /auth/login` but accepts `application/x-www-form-urlencoded`; on success, 302 to `/` (or `?next=` if provided); on failure, re-renders `login.ejs` with an error message via `renderPage()`. Add `app.use(express.urlencoded({ extended: false }))` near the JSON parser in `app.ts`.\n  - Update `src/views/login.ejs` (already exists from R1-04): drop the inline `<script>` fetch handler; change `<form id=\"login-form\">` to `<form method=\"post\" action=\"/login\">`; add a `<p><a href=\"/reset-password\">Forgot your password?</a></p>` link below the submit button. The renderPage helper already passes a `flash` local on redirects from /auth/complete-reset — surface it as a top-of-form notice.\n  - `GET /login` → R1-04 already implemented this in `src/routes/login.ts`. No change.\n  - `GET /reset-password` (no token) → renders `reset-request.ejs`.\n  - `POST /auth/request-reset` → if a user with that email exists, insert a `password_resets` row with a cryptographically random token (32 bytes hex), 1h expiry, and send the reset email via Resend. **Always returns 200 with the same response** regardless of whether the email exists — no enumeration. Log the outcome internally.\n  - `GET /reset-password?token=<t>` → look up the token; if valid + unexpired, render `reset-form.ejs` with the token in a hidden field; otherwise render `reset-request.ejs` with an \"invalid or expired\" message.\n  - `POST /auth/complete-reset` → validate token (exists, not expired, single-use), update `users.password_hash`, delete the `password_resets` row, **delete all sessions for that user** (force re-login on every device), 302 to `/login` with a \"password reset, please log in\" flash.\n- Service additions in `src/services/auth.ts`: `createResetToken(userId)`, `findValidResetToken(token)`, `completeReset(token, newPassword)`, `deleteAllSessionsForUser(userId)`.\n- Tests (vitest): token issue + retrieval; expiry rejection; single-use (token deleted after consume); complete-reset invalidates all sessions for that user; request-reset returns 200 for both existing and non-existing email (no enumeration).\n\nOut of scope here: TOTP / 2FA flow (R1-03), account settings UI (R1-03), rate limiting on /auth/request-reset (R1-03).\n\nVerification: `pnpm test` green. Manual smoke:\n1. `GET /login` → form renders WITHOUT the inline-JS fetch handler (view-source confirms a plain `<form method=\"post\" action=\"/login\">`); the 'Forgot your password?' link is visible and points to `/reset-password`.\n2. POST `/auth/request-reset` for a real email → email arrives in inbox with a link.\n3. Click link → reset-form.ejs renders with token populated.\n4. POST a new password → 302 to /login.\n5. Old password no longer works; new one does. Sessions on any other browser are dead.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (grep for `const COMPLETED_SESSIONS` — single source of truth, no more dual-write). Save in place — the folder is the source of truth and it lives in the `pcblueprint-meta` GitHub repo (auto-deploys to Pages on push).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-03":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm R1-01 + R1-02 + R1-04 merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-03-auth-hardening.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-03 — Auth — Account Settings UI + Optional 2FA + Rate Limiting\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-03 — Auth — Account Settings UI + Optional 2FA + Rate Limiting — pcblueprint-api — ~75 min\n\nRELEASE NOTE (2026-04-25): This session was pulled forward from the original \"Production — Auth Hardening\" phase to R1 (Private Archive Browser). Real auth gates the public archive from day one — no APP_PASSWORD-only window. Scope unchanged; implementation re-targeted from pcblueprint-website (Next.js) to pcblueprint-api (Express + EJS) on 2026-04-25 because R1 ships server-rendered HTML directly from the api. R1 RESCOPE 2026-04-25\n\nAccount self-service UI + optional TOTP 2FA + login/reset rate limiting. Closes R1's auth release.\n\nImplementation in pcblueprint-api:\n- Dependencies: `pnpm add speakeasy qrcode express-rate-limit` (and `@types/qrcode` as dev).\n- EJS view: `views/account.ejs`. Sections:\n  1. Email + last login (from `users` row).\n  2. Change password form → POST /account/change-password (requires current password + new password, calls `hashPassword`, updates row).\n  3. Active sessions list (rows from `sessions` for this user, with user_agent + ip + created_at). Each row has a \"Revoke\" button → POST /account/sessions/:id/revoke.\n  4. \"Sign out everywhere\" button → POST /account/sign-out-everywhere (deletes all sessions for the user; current request gets a fresh cookie pointing at a new session so the user isn't kicked out of the page they're on).\n  5. 2FA section: if `users.totp_secret` is null → \"Enable 2FA\" button (POST /account/2fa/enroll). If set → \"Disable 2FA\" button (POST /account/2fa/disable, requires current password).\n- 2FA enrollment flow:\n  - `POST /account/2fa/enroll` generates a speakeasy secret + the otpauth URL, stores the secret as a PROVISIONAL value in a short-lived in-memory map (or in a temp column `totp_secret_pending`), renders `views/account-2fa-verify.ejs` showing a QR (rendered via qrcode lib as a data URL) + a code input. User scans the QR in their authenticator app and types the current code.\n  - `POST /account/2fa/verify` checks the typed code against the provisional secret. On match, write the secret to `users.totp_secret` and clear the provisional. On miss, re-render with an error.\n- Login flow update (in BOTH `POST /auth/login` (JSON) and `POST /login` (form-post from R1-02) — share the post-password-verify branch via a small helper): after password verify, if `users.totp_secret` is set, **don't create a session yet**. Render `views/login-2fa.ejs` (a code input) with a short-lived intermediate cookie (`pcb_login_pending=<userId>:<expiry>`, 5min). `POST /auth/login-2fa` consumes that cookie + the typed TOTP code; on match, create the real session and redirect — to `/` for HTML clients (or the safely-validated `next=` from the original POST /login if present), or `{ok:true}` JSON for API clients of POST /auth/login.\n- Rate limiting: `express-rate-limit` instances (R1-02 POST-MERGE 2026-04-25 — R1-02 added two new public POSTs that share the brute-force / abuse surface):\n  - `POST /login` (the urlencoded form-post added in R1-02) + `POST /auth/login` (the JSON API entry) + `POST /auth/login-2fa`: 10 requests per minute per IP across the combined set.\n  - `POST /auth/request-reset`: 3 requests per minute per IP, AND additionally throttle by destination email (e.g. 5/hour per email) to prevent inbox flooding even when the attacker rotates IPs.\n  - `POST /auth/complete-reset`: 10 requests per minute per IP (token brute-force defence).\n  - 429 response includes `Retry-After`.\n- Tests (vitest): change-password happy + wrong-current-password rejected; revoke specific session; sign-out-everywhere kills all but current; TOTP enroll+verify happy + wrong-code rejected; rate limiter triggers 429 after threshold.\n\nOut of scope here: WebAuthn / passkeys (future option, post-R1); SMS-based 2FA; admin/multi-user features.\n\nVerification: `pnpm test` green. Manual smoke:\n1. Change password from /account → old fails, new works.\n2. Open a second browser, log in → see two session rows in /account; revoke the second; second browser is logged out on next request.\n3. Enable 2FA → scan QR with authenticator app → verify code → totp_secret saved.\n4. Log out + log in → password prompt → 2FA prompt → success.\n5. Hammer POST /auth/login 11+ times in a minute → 429. Same threshold applies when split across POST /auth/login + POST /login (the limiter is shared across the brute-force surface, not per-URL). Hammer POST /auth/request-reset 4+ times in a minute → 429.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (grep for `const COMPLETED_SESSIONS` — single source of truth, no more dual-write). Save in place — the folder is the source of truth and it lives in the `pcblueprint-meta` GitHub repo (auto-deploys to Pages on push).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-04":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-04-html-render.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-04 — API HTML rendering layer + base layout\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-04 — API HTML rendering layer + base layout — pcblueprint-api — ~60 min\n\nBackground: R1 (Private Archive Browser, MVP-driven plan 2026-04-25) ships server-rendered HTML directly from the API — no Next.js, no MUI, no Tailwind. Default browser CSS only. This session lays the rendering foundation for R1-05..R1-09.\n\nPick a minimal templating engine — recommend EJS (mature, no compile step, plays nicely with Express). Add it as a dependency.\n\nImplement:\n1. Mount the engine: `app.set('view engine', 'ejs')`, views directory at `src/views/`.\n2. Single base layout `views/_layout.ejs` — semantic `<header>` (site title + nav links to /records /writing /raw /source-files /sessions), `<main>` (block: content), `<footer>` (build version + auth status).\n3. One tiny default-CSS sheet at `public/_/runbook.css` (or similar) — fewer than 100 lines, focused on readable typography and table layout. NO framework. NO custom branding.\n4. A `/` route that renders a landing page listing the five buckets with link-throughs.\n5. A 404 page rendered by the engine.\n\nAuth gating: every HTML route (including /) requires a valid session from R1-01. Anonymous requests redirect to /login.\n\nVerification: `pnpm test` green. Manual smoke: visit /, see the bucket list. Visit /records without a session → redirected to /login.\n\nNo actual archive data rendering yet — that's R1-05 / R1-06. This session is pure scaffold + auth-gated.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-05":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-05-index-pages.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-05 — Archive index pages (one route per top-level bucket)\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-05 — Archive index pages (one route per top-level bucket) — pcblueprint-api — ~75 min\n\nBackground: R1-04 landed the rendering layer. This session implements one index page per top-level archive bucket — the user can navigate from / into any bucket and see a list of contents.\n\nReuse note (R1-04 POST-MERGE 2026-04-25): R1-04 already built `src/views/_layout.ejs`, the `renderPage(req, res, next, view, locals?, status?)` helper at `src/views/render.ts`, and the five bucket links on `/` (rendered from `src/views/index.ejs` via `src/routes/home.ts`). Do NOT recreate the layout, the helper, or the home route. Each new bucket route just imports `renderPage` and renders its own per-bucket EJS template — the layout (header/nav/footer + auth status) wraps it automatically.\n\nImplement `GET` routes returning EJS-rendered HTML for:\n- `/records` → grouped sub-list (BP, blood tests, medical notes, exams, medications, flags, blueprint versions). Each group shows the most recent N entries with date + slug, link-throughs to detail pages (R1-06).\n- `/writing` → grouped (journal, memories per author, biography, identity context, identity patterns). Same shape.\n- `/raw` → flat list of raw files with date + filename.\n- `/source-files` → grouped by domain subdir (medical, fitness, etc.) with filename + size + upload date.\n- `/sessions` → reverse-chronological session log entries.\n\nEach list links to detail pages whose URLs are TBD in R1-06 — for this session, render placeholder hrefs (`#`) where the detail page doesn't exist yet, but make the structure right.\n\nPull the data via existing read services (already exist from S04*/S05*) — do NOT re-implement reads here. If a service doesn't yet expose what you need, add a thin reader; do not write to the archive in this session.\n\nVerification: `pnpm test` green. Manual smoke: visit each /index page, see real archive data, view-source confirms it's pure HTML (no JS bundle).\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-06":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-06-detail-pages.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-06 — Record detail pages (per-domain templates)\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-06 — Record detail pages (per-domain templates) — pcblueprint-api — ~75 min\n\nBackground: R1-05 produced index pages with link-throughs to detail pages. This session implements the detail pages.\n\nImplement one EJS template per active record domain, rendered server-side at canonical URLs:\n- `/records/bp/:date-:time-:arm`\n- `/records/blood-tests/:slug`\n- `/records/medical/notes/:date-:slug`\n- `/records/medical/exams/:date-:slug`\n- `/records/medical/medications/:slug`\n- `/records/medical/flags/:slug`\n- `/records/blueprint/:version`\n- `/writing/journal/:date`\n- `/writing/memories/:author/:date-:slug`\n- `/writing/biography` (single page; not slug-indexed)\n- `/writing/identity/context` (single page)\n- `/writing/identity/patterns/:slug`\n- `/raw/:filename`\n- `/source-files/:domain/:filename` — for binaries, render metadata + a download link, never inline the bytes.\n- `/sessions/:id`\n\nPer-domain template renders the JSON / markdown contents in a human-readable format. Tables for tabular data (lab values, BP readings). Markdown rendered with a tiny tool (e.g. `marked`) — no live preview, just static output.\n\nTag chips on every page: render `tags_applied` (or analog field) as small links to `/search?tag=<tag>` (the search page is R1-07).\n\nVerification: `pnpm test` green. Manual smoke: click through index pages from R1-05 into a representative detail page in each domain, verify all fields render.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-07":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-07-search-filter.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-07 — Search + tag filter pages\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-07 — Search + tag filter pages — pcblueprint-api — ~60 min\n\nBackground: R1-05 + R1-06 give navigable browsing. This session adds search.\n\nImplement:\n1. `GET /search` — renders a search form (text input + tag filter dropdown populated from `get_tag_vocabulary()` style read).\n2. `POST /search` (or `GET /search?q=&tag=`) — executes:\n   - **Text query**: full-text scan across journal entries, memories, medical notes, identity context. Use existing search services if any; otherwise add a minimal grep-style scanner over the relevant files (acceptable for R1's data volume — optimise later if needed).\n   - **Tag filter**: filter records whose `tags_applied` (or per-domain tag field) include the selected tag.\n   - **Combined**: AND of both.\n3. Result page renders a flat list of matched records — title, snippet, date, link-through to detail page.\n\nNo JS-side filtering. Form submits, server renders results page.\n\nVerification: `pnpm test` green. Manual smoke: search for a known journal phrase → result. Filter by a known tag → records appear.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-08":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-08-deploy.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-08 — Public deploy (API + HTML as one unit)\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-08 — Public deploy (API + HTML as one unit) — pcblueprint-api — ~60 min\n\nBackground: replaces the original S18a (Vercel website deploy) + S18b (custom domain) — under R1 the archive HTML lives in the API, so deploy is one operation, not two.\n\nChoose a hosting target. Two acceptable paths:\n- **Cloudflare tunnel to NAS** (uses existing infra; lowest cost; api already runs on /volume2).\n- **Fly.io** small instance (more managed; auto-deploys from main; small monthly cost).\n\nRecommend Cloudflare tunnel given existing NAS infra.\n\nImplement:\n1. Production environment config — env vars locked to actual values (API_TOKEN, DB_PATH, RESEND_API_KEY from R1-02, RESET_EMAIL_FROM from R1-02, BUILD_VERSION, etc.). Document required env in README + CLAUDE.md. **Set `BUILD_VERSION` from the GitHub Actions image tag or a Docker build arg** (R1-04 POST-MERGE 2026-04-25) — R1-04 surfaces it in the rendered HTML footer; without this, the prod footer reads `dev` and Pedro can't tell which build is live. **Persist `.data/auth.db` across container restarts** — R1-01 made `AUTH_DB_PATH` configurable but did NOT wire the NAS docker-compose volume mount at `/volume2/docker/api/compose.yaml`. Add the mount here (or the equivalent Fly.io persistent-volume config). Without this, every redeploy logs everyone out and (worse) loses the user rows. Verify by `docker compose down && docker compose up -d` and confirming the existing session cookie still works against the same user. **Wire Resend** (R1-02 POST-MERGE 2026-04-25) — R1-02 added password-reset emails behind `RESEND_API_KEY` (when unset, the link only logs to stdout) and `RESET_EMAIL_FROM` (default is the placeholder `pcblueprint <noreply@pcblueprint.local>` which Resend will reject). Set both env vars in `compose.yaml` and verify the Resend sender domain is verified in the Resend dashboard before going live; otherwise the reset flow is silently broken in production. Smoke test #4 below should request a reset and confirm the email actually arrives.\n2. Cloudflare tunnel config OR Fly.io `fly.toml` — pick one, document the other as the alternative for future reference.\n3. Custom domain (Pedro's choice; document in README) with HTTPS via tunnel / Fly.\n4. Smoke test from public URL: hit /login, complete auth (R1-01..03), browse /records, view a detail page, run a search.\n\nNo CDN, no edge caching tricks — first deploy keeps it boring. Cache headers on static CSS only.\n\nVerification: full end-to-end smoke from a clean browser session. Document the smoke checklist in README so future deploys can re-run it.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only.","R1-09":"STANDING INSTRUCTIONS\n\nBefore starting:\n- Confirm prereqs merged.\n- In pcblueprint-api: git checkout main && git pull origin main.\n- Then: git checkout -b claude/r1-09-observability.\n\nNOTE: Single-repo session. No session-log entry — batched into S12.\n\nBefore ending the session you MUST:\n- Update README.md + CLAUDE.md if conventions / endpoints / env vars changed.\n- Push branch + open PR titled \"R1-09 — Observability minimum (request log + crash reporting)\".\n- Do not merge without my approval.\n\n\nSECURITY RULES (added 2026-04-24, follow strictly):\n- Never paste discovered secret values (API keys, tokens, passwords) into chat responses or PR descriptions. Reference by variable name only (e.g. \"the API_KEY env var\"), never the value itself.\n- When investigating env/config, prefer variable-name-only output. If a value must be revealed to answer a question, warn the user first and confirm before showing it.\n- Do not modify package-lock.json. If npm install drifts the lockfile, discard (git checkout --) rather than commit — the repo has pre-existing drift that should be resolved in a dedicated PR.\n\nDEFENSIVE RULES (follow strictly):\n- Never read files >30KB.\n- Use git mv only for file moves.\n- Don't boot the dev server; leave runtime verification to me.\n- On test failure, read only the failing test's output.\n- Commit in small chunks, not one mega-commit.\n- If scope is wider than expected, STOP and hand back a note.\n\n---\n\nSESSION R1-09 — Observability minimum (request log + crash reporting) — pcblueprint-api — ~45 min\n\nBackground: R1 is now publicly reachable. Operability needs request-level visibility.\n\nImplement (minimum viable, NOT the full S16a observability):\n1. Structured request log — one JSON line per request with: timestamp, method, path, status, duration_ms, user_id (if authenticated), error_code (if any). Write to `logs/requests-<YYYY-MM-DD>.ndjson`. Rotate daily.\n2. Crash reporting — uncaught exceptions + 5xx responses captured with stack trace into `logs/errors-<YYYY-MM-DD>.ndjson`.\n3. `GET /healthz` — returns 200 with build version + uptime + last-error timestamp. Public (no auth).\n4. Daily logrotate-style archival into `logs/archive/` after 7 days; delete after 90 days.\n\nDo NOT add Sentry / Datadog / external SaaS in this session. NDJSON logs on disk are enough for R1 launch — escalate if/when volume justifies.\n\nVerification: induce a 5xx (e.g. force a route to throw), confirm crash log captures stack. Confirm /healthz returns 200. Check log rotation by setting the date forward in a test.\n\n---\n**Closing checklist (standing rule — applies after this session's PR is merged to main; for manual sessions, after you mark complete in the runbook):**\n\n1. Open `Blueprint Tasks\\pcblueprint-checklist_25 april.html` and add this session's id to the `COMPLETED_SESSIONS` array (~line 2135 — single source of truth, no more dual-write). Save in place — the folder is the source of truth and Pedro syncs it manually between machines (now also via the `pcblueprint-meta` GitHub repo).\n2. Write the increment in scrum-master voice — *what shippable capability Pedro now has, plus accepted trade-offs/follow-ups*. Land it in TWO places: (a) the touched repo's `CHANGELOG.md` as a new `### Increment` paragraph in this session's dated entry; (b) the runbook HTML's `📒 Changelog` block as a single one-liner.\n3. Walk the remaining not-done sessions in `SESSIONS`. For each one whose assumptions/scope/prereqs/necessity changed because of this merge, output one bullet to Pedro: `<Sxx | R1-xx> — <Title>: <recommendation> — <one-sentence reason>`. Do NOT silently edit other prompts; recommend only."}
